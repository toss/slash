import { css } from '@emotion/react';

/**
 * @description `<a>`의 기본 스타일을 제거하는 유틸리티입니다.
 *
 * ```ts
 * const resetAnchor: SerializedStyles;
 * ```
 */
export const resetAnchor = css`
  text-decoration: none;
`;
